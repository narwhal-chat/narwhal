import React from 'react';

import styles from './ChatForm.css';

const form = (props) => {
  return (
    <div className={styles.ChatForm}>
      <div>Header</div>
      <div>...</div>
    </div>
  );
};

export default form;